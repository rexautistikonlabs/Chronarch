[project]
name = "chronarch-nervous"
version = "0.0.1"
description = "Chronarch nervous package"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
